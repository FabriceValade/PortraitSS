<UserControl x:Class="PortraitEditor.View.ModView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PortraitEditor.View"
             mc:Ignorable="d"
             Background="White">
    <!--d:DesignHeight="300" d:DesignWidth="300"-->
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Reparation society" Margin="10,3,10,3" Foreground="Black" FontSize="18" FontWeight="Bold" VerticalAlignment="Bottom"/>
        <Border Grid.Column="1" BorderBrush="Black" BorderThickness="1" CornerRadius="15" VerticalAlignment="Bottom">
        <TextBlock Name="FactionCounter"  Text="100" Margin="3,3,3,3" FontSize="18" VerticalAlignment="Bottom"/>
        </Border>
        <ContentControl Grid.Row="0" Grid.Column="2" Content="{Binding Path=Url}" HorizontalAlignment="Stretch" Margin="10,0,10,0" VerticalAlignment="Bottom"></ContentControl>
    </Grid>
</UserControl>
