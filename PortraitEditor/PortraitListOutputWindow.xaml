<Window x:Class="PortraitEditor.PortraitListOutputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PortraitEditor"
        mc:Ignorable="d"
        Title="PortraitListOutputWindow" Height="300" Width="300">
    <StackPanel>
        <Button Click="Button_Click" Width="50"> Ok </Button>
        <ListView Name="PortraitList" Grid.Column="1" Grid.Row="0" ItemsSource="{Binding Path=DisplayPortrait}"  Style="{StaticResource PortraitListviewStyle}"  >
            <ListView.ItemContainerStyle>
                <Style TargetType="{x:Type ListViewItem}">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ImageGender}" Value="Male">
                            <Setter Property="Background" Value="Blue" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding ImageGender}" Value="Female">
                            <Setter Property="Background" Value="Yellow" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>
    </StackPanel>
</Window>
